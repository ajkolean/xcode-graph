## API Report File for "tuistgraph"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Bindable } from '@zag-js/core';
import { BindableContext } from '@zag-js/core';
import type { BindableParams } from '@zag-js/core';
import { BindableRefs } from '@zag-js/core';
import { ComputedFn } from '@zag-js/core';
import { Machine } from '@zag-js/core';
import { MachineSchema } from '@zag-js/core';
import { MachineStatus } from '@zag-js/core';
import { PropFn } from '@zag-js/core';
import { Scope } from '@zag-js/core';
import { Service } from '@zag-js/core';
import { StoreApi } from 'zustand';
import { UseBoundStore } from 'zustand';
import { z } from 'zod';

// @public
export type ActiveTab = 'overview' | 'builds' | 'test-runs' | 'module-cache' | 'xcode-cache' | 'previews' | 'qa' | 'bundles' | 'graph';

// @public
export interface AdjacencyList {
    // (undocumented)
    forward: Map<string, string[]>;
    // (undocumented)
    reverse: Map<string, string[]>;
}

// @public
export function adjustColorForZoom(color: string, zoom: number): string;

// @public
export function adjustOpacityForZoom(baseOpacity: number, zoom: number): number;

// @public
export function analyzeCluster(cluster: Cluster, allEdges: GraphEdge[]): void;

// @public
export function applyCollisionForces<T extends CollisionEntity>(entities: T[], alpha: number, config: CollisionConfig): void;

// @public (undocumented)
export function applyGraphFilters(nodes: GraphNode[], edges: GraphEdge[], filters: FilterState, searchQuery: string): {
    filteredNodes: {
        id: string;
        name: string;
        type: "app" | "framework" | "library" | "test-unit" | "test-ui" | "cli" | "package";
        platform: "iOS" | "macOS" | "visionOS" | "tvOS" | "watchOS";
        origin: "local" | "external";
        project?: string;
        targetCount?: number;
    }[];
    filteredEdges: {
        source: string;
        target: string;
    }[];
    searchResults: number;
};

// @public
export function arrangeClusterGrid(clusters: Cluster[], config?: ClusterLayoutConfig): Map<string, {
    x: number;
    y: number;
}>;

// @public
export function assignLayers(nodes: string[], edges: Array<{
    from: string;
    to: string;
}>): Record<string, number>;

// @public
export function bindable<T>(props: () => BindableParams<T>): Bindable<T>;

// @public
export function buildAdjacency(nodes: string[], edges: Array<{
    from: string;
    to: string;
}>): AdjacencyList;

// @public
export function calculateBoundingRadius(width: number, height: number): number;

// @public
export function calculateViewportBounds(svgWidth: number, svgHeight: number, panX: number, panY: number, zoom: number, margin?: number): ViewportBounds;

// @public
export interface Cluster extends Omit<z.infer<typeof ClusterSchema>, 'metadata'> {
    // (undocumented)
    metadata: Map<string, ClusterNodeMetadata>;
}

// @public
export const CLUSTER_TYPE_VALUES: ("package" | "project")[];

// @public
export type ClusterBounds = z.infer<typeof ClusterBoundsSchema>;

// @public
export const ClusterBoundsSchema: z.ZodObject<{
    x: z.ZodNumber;
    y: z.ZodNumber;
    width: z.ZodNumber;
    height: z.ZodNumber;
}, z.core.$strip>;

// @public
export type ClusterLayoutConfig = z.infer<typeof ClusterLayoutConfigSchema>;

// @public
export const ClusterLayoutConfigSchema: z.ZodObject<{
    clusterPadding: z.ZodNumber;
    clusterSpacing: z.ZodNumber;
    layerSpacing: z.ZodNumber;
    minNodeSpacing: z.ZodNumber;
    ringRadius: z.ZodNumber;
    anchorNodeSize: z.ZodNumber;
    normalNodeSize: z.ZodNumber;
    testNodeSize: z.ZodNumber;
    testOrbitRadius: z.ZodNumber;
    forceStrength: z.ZodObject<{
        boundary: z.ZodNumber;
        radial: z.ZodNumber;
        collision: z.ZodNumber;
        anchor: z.ZodNumber;
        testSatellite: z.ZodNumber;
    }, z.core.$strip>;
}, z.core.$strip>;

// @public (undocumented)
export interface ClusterLayoutResult {
    // (undocumented)
    clusterId: string;
    // (undocumented)
    height: number;
    // (undocumented)
    layer: number;
    // (undocumented)
    projectIds: string[];
    // (undocumented)
    width: number;
    // (undocumented)
    x: number;
    // (undocumented)
    y: number;
}

// @public
export type ClusterNodeMetadata = z.infer<typeof ClusterNodeMetadataSchema>;

// @public
export const ClusterNodeMetadataSchema: z.ZodObject<{
    nodeId: z.ZodString;
    role: z.ZodEnum<{
        entry: "entry";
        "internal-framework": "internal-framework";
        "internal-lib": "internal-lib";
        utility: "utility";
        test: "test";
        tool: "tool";
    }>;
    layer: z.ZodNumber;
    isAnchor: z.ZodBoolean;
    hasExternalDependents: z.ZodBoolean;
    testSubjects: z.ZodOptional<z.ZodArray<z.ZodString>>;
    dependencyCount: z.ZodNumber;
    dependsOnCount: z.ZodNumber;
}, z.core.$strip>;

// @public
export type ClusterPosition = z.infer<typeof ClusterPositionSchema>;

// @public
export const ClusterPositionSchema: z.ZodObject<{
    id: z.ZodString;
    x: z.ZodNumber;
    y: z.ZodNumber;
    vx: z.ZodNumber;
    vy: z.ZodNumber;
    width: z.ZodNumber;
    height: z.ZodNumber;
    nodeCount: z.ZodNumber;
}, z.core.$strip>;

// @public
export const ClusterSchema: z.ZodObject<{
    id: z.ZodString;
    name: z.ZodString;
    type: z.ZodEnum<{
        package: "package";
        project: "project";
    }>;
    origin: z.ZodEnum<{
        local: "local";
        external: "external";
    }>;
    nodes: z.ZodArray<z.ZodObject<{
        id: z.ZodString;
        name: z.ZodString;
        type: z.ZodEnum<{
            app: "app";
            framework: "framework";
            library: "library";
            "test-unit": "test-unit";
            "test-ui": "test-ui";
            cli: "cli";
            package: "package";
        }>;
        platform: z.ZodEnum<{
            iOS: "iOS";
            macOS: "macOS";
            visionOS: "visionOS";
            tvOS: "tvOS";
            watchOS: "watchOS";
        }>;
        origin: z.ZodEnum<{
            local: "local";
            external: "external";
        }>;
        project: z.ZodOptional<z.ZodString>;
        targetCount: z.ZodOptional<z.ZodNumber>;
    }, z.core.$strip>>;
    anchors: z.ZodArray<z.ZodString>;
    bounds: z.ZodOptional<z.ZodObject<{
        x: z.ZodNumber;
        y: z.ZodNumber;
        width: z.ZodNumber;
        height: z.ZodNumber;
    }, z.core.$strip>>;
}, z.core.$strip>;

// @public
export type ClusterType = z.infer<typeof ClusterTypeSchema>;

// @public
export const ClusterTypeSchema: z.ZodEnum<{
    package: "package";
    project: "project";
}>;

// @public
export interface CollisionConfig {
    // (undocumented)
    damping?: number;
    // (undocumented)
    forceStrength: number;
    // (undocumented)
    minDistance?: number;
    // (undocumented)
    separationPadding: number;
}

// @public
export interface CollisionEntity {
    // (undocumented)
    id: string;
    // (undocumented)
    radius: number;
    // (undocumented)
    vx?: number;
    // (undocumented)
    vy?: number;
    // (undocumented)
    x: number;
    // (undocumented)
    y: number;
}

// @public
export const CollisionPresets: {
    readonly NODE_COLLISION: {
        readonly minDistance: 100;
        readonly separationPadding: 8;
        readonly forceStrength: 0.3;
        readonly damping: 0.7;
    };
    readonly CLUSTER_SPACING: {
        readonly minDistance: number;
        readonly separationPadding: 80;
        readonly forceStrength: 0.4;
        readonly damping: 0.7;
    };
    readonly GENTLE: {
        readonly minDistance: 100;
        readonly separationPadding: 10;
        readonly forceStrength: 0.2;
        readonly damping: 0.8;
    };
    readonly STRONG: {
        readonly minDistance: 150;
        readonly separationPadding: 20;
        readonly forceStrength: 0.6;
        readonly damping: 0.5;
    };
};

// @public
export function computeClusterLayout(clusterIds: string[], clusterEdges: Array<{
    from: string;
    to: string;
}>, clusterDimensions?: Map<string, number>): {
    layouts: ClusterLayoutResult[];
    superNodes: SuperNode[];
    superEdges: SuperEdge[];
};

// Warning: (ae-forgotten-export) The symbol "ClusterStatsResult" needs to be exported by the entry point api.d.ts
//
// @public
export function computeClusterStats(clusterNodes: GraphNode[], edges: GraphEdge[], filteredEdges?: GraphEdge[]): ClusterStatsResult;

// @public
export function computeElasticShellRadius(nodes: NodeWithPosition[], masses: Map<string, NodeMass>, config?: ElasticShellConfig): number;

// @public
export function computeFilters(allNodes: GraphNode[]): {
    typeCounts: Map<string, number>;
    platformCounts: Map<string, number>;
    projectCounts: Map<string, number>;
    packageCounts: Map<string, number>;
    hasActiveFilters: (filters: FilterState) => boolean;
    createClearFilters: (onFiltersChange: (filters: FilterState) => void) => () => void;
};

// @public
export function computeHierarchicalLayout(nodes: GraphNode[], edges: GraphEdge[], clusters: Cluster[]): HierarchicalLayoutResult;

// @public
export function computeMEC(positions: NodeCartesian[], centerX: number, centerY: number, masses?: Map<string, NodeMass>): number;

// @public
export function computeNodeDependencies(node: GraphNode | null, allNodes: GraphNode[], edges: GraphEdge[], filteredEdges?: GraphEdge[]): {
    dependencies: {
        id: string;
        name: string;
        type: "app" | "framework" | "library" | "test-unit" | "test-ui" | "cli" | "package";
        platform: "iOS" | "macOS" | "visionOS" | "tvOS" | "watchOS";
        origin: "local" | "external";
        project?: string;
        targetCount?: number;
    }[];
    dependents: {
        id: string;
        name: string;
        type: "app" | "framework" | "library" | "test-unit" | "test-ui" | "cli" | "package";
        platform: "iOS" | "macOS" | "visionOS" | "tvOS" | "watchOS";
        origin: "local" | "external";
        project?: string;
        targetCount?: number;
    }[];
    metrics: {
        dependencyCount: number;
        dependentCount: number;
        totalDependencyCount: number;
        totalDependentCount: number;
        isHighFanIn: boolean;
        isHighFanOut: boolean;
        totalConnections: number;
    };
};

// @public
export function computeNodeMasses(nodes: Array<{
    id: string;
    type: string;
}>, edges: Array<{
    from: string;
    to: string;
}>): Map<string, NodeMass>;

// @public (undocumented)
export function computeTransitiveDependencies(viewMode: ViewMode, selectedNode: GraphNode | null, edges: GraphEdge[]): {
    transitiveDeps: TransitiveResult;
    transitiveDependents: TransitiveResult;
};

// @public
export function condenseToDAG(clusterIds: string[], edges: Array<{
    from: string;
    to: string;
}>): {
    superNodes: SuperNode[];
    superEdges: SuperEdge[];
    nodeToSccId: Map<string, string>;
};

// @public
export function createSpatialHash<T extends SpatialEntity>(entities: T[], cellSize?: number): SpatialHash<T>;

// @public
export interface DataStore {
    _cachedDisplayData: DisplayData | null;
    _cachedFilteredData: FilteredData | null;
    clearCache: () => void;
    edges: GraphEdge[];
    getDisplayData: (filters: FilterState, searchQuery: string, viewMode: ViewMode, selectedNode: GraphNode | null) => DisplayData;
    getFilteredData: (filters: FilterState, searchQuery: string) => FilteredData;
    _lastFilters: FilterState | null;
    _lastSearchQuery: string;
    _lastSelectedNodeId: string | null;
    _lastViewMode: ViewMode;
    nodes: GraphNode[];
    setGraphData: (nodes: GraphNode[], edges: GraphEdge[]) => void;
}

// @public
export const DEFAULT_CLUSTER_CONFIG: ClusterLayoutConfig;

// @public
export function determineRole(node: GraphNode, isTest: boolean, isAnchor: boolean, dependentCount: number): NodeRole;

// @public
export interface DisplayData extends FilteredData {
    transitiveDependents: TransitiveResult;
    transitiveDeps: TransitiveResult;
}

// @public (undocumented)
export interface ElasticShellConfig {
    // (undocumented)
    alpha: number;
    // (undocumented)
    baseRadius: number;
    // (undocumented)
    compressionFactor: number;
    // (undocumented)
    iterations: number;
    // (undocumented)
    maxRadius: number;
    // (undocumented)
    minRadius: number;
}

// @public
export interface ExtendedEvent<T extends MachineSchema> {
    // (undocumented)
    [key: string]: unknown;
    // (undocumented)
    current: () => MachineEvent;
    // (undocumented)
    previous: () => MachineEvent | null;
    // (undocumented)
    type: string;
}

// @public
export interface ExtendedState<T extends MachineSchema> extends Bindable<T['state']> {
    // (undocumented)
    hasTag: (tag: T['tag']) => boolean;
    // (undocumented)
    matches: (...values: T['state'][]) => boolean;
}

// @public
export interface FilteredData {
    filteredEdges: GraphEdge[];
    filteredNodes: GraphNode[];
    searchResults: number | null;
}

// @public
export type FilterState = z.output<typeof FilterStateSchema>;

// @public
export type FilterStateInput = z.infer<typeof FilterStateInputSchema>;

// @public
export const FilterStateInputSchema: z.ZodObject<{
    nodeTypes: z.ZodArray<z.ZodString>;
    platforms: z.ZodArray<z.ZodString>;
    origins: z.ZodArray<z.ZodString>;
    projects: z.ZodArray<z.ZodString>;
    packages: z.ZodArray<z.ZodString>;
}, z.core.$strip>;

// @public
export const FilterStateSchema: z.ZodPipe<z.ZodObject<{
    nodeTypes: z.ZodArray<z.ZodString>;
    platforms: z.ZodArray<z.ZodString>;
    origins: z.ZodArray<z.ZodString>;
    projects: z.ZodArray<z.ZodString>;
    packages: z.ZodArray<z.ZodString>;
}, z.core.$strip>, z.ZodTransform<{
    nodeTypes: Set<string>;
    platforms: Set<string>;
    origins: Set<string>;
    projects: Set<string>;
    packages: Set<string>;
}, {
    nodeTypes: string[];
    platforms: string[];
    origins: string[];
    projects: string[];
    packages: string[];
}>>;

// @public
export interface FilterStore {
    allPackages: Set<string>;
    allProjects: Set<string>;
    clearFilters: (defaults: FilterState) => void;
    filters: FilterState;
    initializeFromData: (projects: Set<string>, packages: Set<string>) => void;
    searchQuery: string;
    setFilters: (filters: FilterState) => void;
    setSearchQuery: (query: string) => void;
    toggleNodeType: (type: string) => void;
    toggleOrigin: (origin: string) => void;
    togglePackage: (pkg: string) => void;
    togglePlatform: (platform: string) => void;
    toggleProject: (project: string) => void;
}

// @public
export type ForceStrength = z.infer<typeof ForceStrengthSchema>;

// @public
export const ForceStrengthSchema: z.ZodObject<{
    boundary: z.ZodNumber;
    radial: z.ZodNumber;
    collision: z.ZodNumber;
    anchor: z.ZodNumber;
    testSatellite: z.ZodNumber;
}, z.core.$strip>;

// @public
export function generateColor(input: string, category?: string): string;

// @public
export function generateColorMap<T extends string>(keys: Iterable<T>, category: 'platform' | 'project' | 'package'): Map<T, string>;

// @public
export function generateColorPalette(items: string[], category?: string): Map<string, string>;

// @public
export function generateColorWithAlpha(input: string, alpha: number, category?: string): string;

// @public
export function getBaseNodeSize(type: string): number;

// @public
export function getConnectedNodeIds(selectedNode: GraphNode | null, edges: GraphEdge[]): Set<string>;

// @public
export function getConnectedNodes(nodeId: string, edges: GraphEdge[]): Set<string>;

// @public
export function getConnectionCount(nodeId: string, edges: GraphEdge[]): number;

// @public
export function getDependencyCount(nodeId: string, edges: GraphEdge[]): number;

// @public
export function getDependentCount(nodeId: string, edges: GraphEdge[]): number;

// @public
export function getNodeIconPath(type: string, platform?: string): string;

// @public
export function getNodeSize(node: GraphNode, edges: GraphEdge[]): number;

// @public
export function getNodeTypeColor(type: string): string;

// @public
export function getNodeTypeLabel(type: string): string;

// @public
export function getPlatformIconPath(platform: string): string;

// @public
export type GraphData = z.infer<typeof GraphDataSchema>;

// @public
export const GraphDataSchema: z.ZodObject<{
    nodes: z.ZodArray<z.ZodObject<{
        id: z.ZodString;
        name: z.ZodString;
        type: z.ZodEnum<{
            app: "app";
            framework: "framework";
            library: "library";
            "test-unit": "test-unit";
            "test-ui": "test-ui";
            cli: "cli";
            package: "package";
        }>;
        platform: z.ZodEnum<{
            iOS: "iOS";
            macOS: "macOS";
            visionOS: "visionOS";
            tvOS: "tvOS";
            watchOS: "watchOS";
        }>;
        origin: z.ZodEnum<{
            local: "local";
            external: "external";
        }>;
        project: z.ZodOptional<z.ZodString>;
        targetCount: z.ZodOptional<z.ZodNumber>;
    }, z.core.$strip>>;
    edges: z.ZodArray<z.ZodObject<{
        source: z.ZodString;
        target: z.ZodString;
    }, z.core.$strip>>;
}, z.core.$strip>;

// @public (undocumented)
export class GraphDataService {
    constructor(nodes: GraphNode[], edges: GraphEdge[]);
    findCircularDependencies(): string[][];
    getAllEdges(): GraphEdge[];
    getAllNodes(): GraphNode[];
    getAllPackages(): Set<string>;
    getAllProjects(): Set<string>;
    getCluster(clusterId: string): Cluster | null;
    getClusterNodes(clusterId: string): GraphNode[];
    getClusterStats(clusterId: string): {
        nodeCount: number;
        dependencies: number;
        dependents: number;
        platforms: Set<string>;
    };
    getDirectDependencies(nodeId: string): GraphNode[];
    getDirectDependents(nodeId: string): GraphNode[];
    getEdge(source: string, target: string): GraphEdge | undefined;
    getGraphStats(): {
        totalNodes: number;
        totalEdges: number;
        avgDependencies: number;
        maxDependencies: number;
        isolatedNodes: number;
    };
    getIncomingEdges(nodeId: string): GraphEdge[];
    getNodeById(id: string): GraphNode | undefined;
    getNodeEdges(nodeId: string): GraphEdge[];
    getNodesByOrigin(origin: 'local' | 'external'): GraphNode[];
    getNodesByPlatform(platform: string): GraphNode[];
    getNodesByProject(project: string): GraphNode[];
    getNodesByType(type: string): GraphNode[];
    getNodeStats(nodeId: string): {
        dependencies: number;
        dependents: number;
        transitiveDeps: number;
        transitiveDependents: number;
    };
    getOutgoingEdges(nodeId: string): GraphEdge[];
    getTransitiveDependencies(nodeId: string): {
        nodes: Set<string>;
        edges: Set<string>;
        depths: Map<string, number>;
    };
    getTransitiveDependents(nodeId: string): {
        nodes: Set<string>;
        edges: Set<string>;
        depths: Map<string, number>;
    };
    hasPath(fromId: string, toId: string): boolean;
    searchNodes(query: string): GraphNode[];
}

// @public
export type GraphEdge = z.infer<typeof GraphEdgeSchema>;

// @public
export const GraphEdgeSchema: z.ZodObject<{
    source: z.ZodString;
    target: z.ZodString;
}, z.core.$strip>;

// @public (undocumented)
export class GraphLoader {
    constructor(config?: ProgressiveLoadConfig);
    estimateLoadTime(nodeCount: number): {
        chunks: number;
        estimatedMs: number;
        recommendation: 'instant' | 'fast' | 'progressive';
    };
    loadByClusterPriority(clusters: Cluster[], nodes: GraphNode[], edges: GraphEdge[], onProgress: (progress: LoadProgress) => void): Promise<{
        nodes: GraphNode[];
        edges: GraphEdge[];
    }>;
    loadGraphProgressive(nodes: GraphNode[], edges: GraphEdge[], onProgress: (progress: LoadProgress) => void): Promise<{
        nodes: GraphNode[];
        edges: GraphEdge[];
    }>;
}

// @public
export type GraphNode = z.infer<typeof GraphNodeSchema>;

// @public
export const GraphNodeSchema: z.ZodObject<{
    id: z.ZodString;
    name: z.ZodString;
    type: z.ZodEnum<{
        app: "app";
        framework: "framework";
        library: "library";
        "test-unit": "test-unit";
        "test-ui": "test-ui";
        cli: "cli";
        package: "package";
    }>;
    platform: z.ZodEnum<{
        iOS: "iOS";
        macOS: "macOS";
        visionOS: "visionOS";
        tvOS: "tvOS";
        watchOS: "watchOS";
    }>;
    origin: z.ZodEnum<{
        local: "local";
        external: "external";
    }>;
    project: z.ZodOptional<z.ZodString>;
    targetCount: z.ZodOptional<z.ZodNumber>;
}, z.core.$strip>;

// @public
export interface GraphStore {
    circularDependencies: string[][];
    focusNode: (node: GraphNode) => void;
    hoveredNode: string | null;
    resetView: () => void;
    selectCluster: (clusterId: string | null) => void;
    selectedCluster: string | null;
    selectedNode: GraphNode | null;
    selectNode: (node: GraphNode | null) => void;
    setCircularDependencies: (cycles: string[][]) => void;
    setHoveredNode: (nodeId: string | null) => void;
    setViewMode: (mode: ViewMode) => void;
    showDependents: (node: GraphNode) => void;
    showImpact: (node: GraphNode) => void;
    viewMode: ViewMode;
}

// @public
export function groupIntoClusters(nodes: GraphNode[], edges: GraphEdge[]): Cluster[];

// @public
export interface HierarchicalLayoutResult {
    // (undocumented)
    clusterPositions: Map<string, ClusterPosition>;
    // (undocumented)
    clusters: Cluster[];
    // (undocumented)
    nodePositions: Map<string, NodePosition>;
}

// @public
export function identifyAnchors(nodes: GraphNode[], dependents: Map<string, Set<string>>, externalDependents: Map<string, number>): GraphNode[];

// Warning: (ae-forgotten-export) The symbol "Point" needs to be exported by the entry point api.d.ts
//
// @public
export function isCircleInViewport(center: Point, radius: number, bounds: ViewportBounds): boolean;

// @public
export function isLineInViewport(start: Point, end: Point, bounds: ViewportBounds): boolean;

// @public
export interface LoadProgress {
    chunk?: {
        nodes: GraphNode[];
        edges: GraphEdge[];
    };
    loadedEdges: number;
    loadedNodes: number;
    percentage: number;
    totalEdges: number;
    totalNodes: number;
    type: 'chunk' | 'complete';
}

// @public
export interface MachineEvent {
    // (undocumented)
    [key: string]: unknown;
    // (undocumented)
    type: string;
}

// @public
export interface MachineUserProps {
    // (undocumented)
    [key: string]: unknown;
    // (undocumented)
    getRootNode?: () => ShadowRoot | Document | Node;
    // (undocumented)
    id?: string;
    // (undocumented)
    ids?: Record<string, string>;
}

// @public
export function matchesSearch(node: GraphNode, searchQuery: string): boolean;

// @public
export const NODE_ROLE_VALUES: ("entry" | "internal-framework" | "internal-lib" | "utility" | "test" | "tool")[];

// @public
export const NODE_TYPE_COLORS: Record<string, string>;

// @public
export const NODE_TYPE_VALUES: ("app" | "framework" | "library" | "test-unit" | "test-ui" | "cli" | "package")[];

// @public
export interface NodeCartesian {
    // (undocumented)
    id: string;
    // (undocumented)
    isTest: boolean;
    // (undocumented)
    ring: number;
    // (undocumented)
    x: number;
    // (undocumented)
    y: number;
}

// @public
export interface NodeMass {
    // (undocumented)
    centrality: number;
    // (undocumented)
    depth: number;
    // (undocumented)
    fanIn: number;
    // (undocumented)
    fanOut: number;
    // (undocumented)
    mass: number;
    // (undocumented)
    nodeId: string;
}

// @public
export type NodePosition = z.infer<typeof NodePositionSchema>;

// @public
export const NodePositionSchema: z.ZodObject<{
    id: z.ZodString;
    x: z.ZodNumber;
    y: z.ZodNumber;
    vx: z.ZodNumber;
    vy: z.ZodNumber;
    clusterId: z.ZodString;
    radius: z.ZodNumber;
    targetRadius: z.ZodOptional<z.ZodNumber>;
    targetAngle: z.ZodOptional<z.ZodNumber>;
    isAnchor: z.ZodOptional<z.ZodBoolean>;
    isTest: z.ZodOptional<z.ZodBoolean>;
    testSubject: z.ZodOptional<z.ZodString>;
}, z.core.$strip>;

// @public
export type NodeRole = z.infer<typeof NodeRoleSchema>;

// @public
export const NodeRoleSchema: z.ZodEnum<{
    entry: "entry";
    "internal-framework": "internal-framework";
    "internal-lib": "internal-lib";
    utility: "utility";
    test: "test";
    tool: "tool";
}>;

// @public
export type NodeType = z.infer<typeof NodeTypeSchema>;

// @public
export const NodeTypeSchema: z.ZodEnum<{
    app: "app";
    framework: "framework";
    library: "library";
    "test-unit": "test-unit";
    "test-ui": "test-ui";
    cli: "cli";
    package: "package";
}>;

// @public (undocumented)
export interface NodeWithPosition {
    // (undocumented)
    id: string;
    // (undocumented)
    mass?: number;
    // (undocumented)
    ring: number;
    // (undocumented)
    x: number;
    // (undocumented)
    y: number;
}

// @public
export type Origin = z.infer<typeof OriginSchema>;

// @public
export const ORIGIN_VALUES: ("local" | "external")[];

// @public
export const OriginSchema: z.ZodEnum<{
    local: "local";
    external: "external";
}>;

// @public
export type Platform = z.infer<typeof PlatformSchema>;

// @public
export const PLATFORM_COLOR = "#6F2CFF";

// @public
export const PLATFORM_VALUES: ("iOS" | "macOS" | "visionOS" | "tvOS" | "watchOS")[];

// @public
export const PlatformSchema: z.ZodEnum<{
    iOS: "iOS";
    macOS: "macOS";
    visionOS: "visionOS";
    tvOS: "tvOS";
    watchOS: "watchOS";
}>;

// @public
export type PositionedNode = z.infer<typeof PositionedNodeSchema>;

// @public
export const PositionedNodeSchema: z.ZodObject<{
    node: z.ZodObject<{
        id: z.ZodString;
        name: z.ZodString;
        type: z.ZodEnum<{
            app: "app";
            framework: "framework";
            library: "library";
            "test-unit": "test-unit";
            "test-ui": "test-ui";
            cli: "cli";
            package: "package";
        }>;
        platform: z.ZodEnum<{
            iOS: "iOS";
            macOS: "macOS";
            visionOS: "visionOS";
            tvOS: "tvOS";
            watchOS: "watchOS";
        }>;
        origin: z.ZodEnum<{
            local: "local";
            external: "external";
        }>;
        project: z.ZodOptional<z.ZodString>;
        targetCount: z.ZodOptional<z.ZodNumber>;
    }, z.core.$strip>;
    x: z.ZodNumber;
    y: z.ZodNumber;
    clusterId: z.ZodString;
    metadata: z.ZodObject<{
        nodeId: z.ZodString;
        role: z.ZodEnum<{
            entry: "entry";
            "internal-framework": "internal-framework";
            "internal-lib": "internal-lib";
            utility: "utility";
            test: "test";
            tool: "tool";
        }>;
        layer: z.ZodNumber;
        isAnchor: z.ZodBoolean;
        hasExternalDependents: z.ZodBoolean;
        testSubjects: z.ZodOptional<z.ZodArray<z.ZodString>>;
        dependencyCount: z.ZodNumber;
        dependsOnCount: z.ZodNumber;
    }, z.core.$strip>;
    localX: z.ZodOptional<z.ZodNumber>;
    localY: z.ZodOptional<z.ZodNumber>;
    targetRadius: z.ZodOptional<z.ZodNumber>;
    targetAngle: z.ZodOptional<z.ZodNumber>;
}, z.core.$strip>;

// @public
export type PreviewFilter = {
    type: 'nodeType' | 'platform' | 'origin' | 'project' | 'package' | 'cluster';
    value: string;
} | null;

// @public
export interface ProgressiveLoadConfig {
    chunkSize?: number;
    delayBetweenChunks?: number;
    priorityClusterIds?: string[];
}

// @public
export function selectMassBasedAnchor(nodes: Array<{
    id: string;
    type: string;
}>, edges: Array<{
    from: string;
    to: string;
}>): string | null;

// @public
export function serializeFilterState(state: FilterState): FilterStateInput;

// @public
export function shouldDimNode(node: GraphNode, selectedNode: GraphNode | null, connectedNodes: Set<string>, searchQuery: string): boolean;

// @public
export function shouldShowEdge(edge: GraphEdge, selectedNode: GraphNode | null, searchQuery: string, nodes: GraphNode[]): boolean;

// @public
export const sidebarMachine: Machine<SidebarMachineSchema>;

// @public
export interface SidebarMachineSchema extends MachineSchema {
    // (undocumented)
    action: 'setSelectedNode' | 'setSelectedCluster' | 'clearSelectedNode' | 'clearSelectedCluster' | 'clearSelections' | 'switchToNodeDetails' | 'switchToClusterDetails' | 'switchToFilters' | 'switchTab' | 'toggleSection' | 'expandSection' | 'notifyCollapseChange';
    // (undocumented)
    context: {
        activeTab: SidebarTab;
        expandedSections: Record<SidebarSection, boolean>;
        selectedNodeId: string | null;
        selectedClusterId: string | null;
    };
    // (undocumented)
    event: {
        type: 'TOGGLE';
    } | {
        type: 'EXPAND';
    } | {
        type: 'COLLAPSE';
    } | {
        type: 'SELECT_NODE';
        nodeId: string;
    } | {
        type: 'SELECT_CLUSTER';
        clusterId: string;
    } | {
        type: 'CLEAR_SELECTION';
    } | {
        type: 'SWITCH_TAB';
        tab: SidebarTab;
    } | {
        type: 'TOGGLE_SECTION';
        section: SidebarSection;
    } | {
        type: 'EXPAND_TO_SECTION';
        section: SidebarSection;
    };
    // (undocumented)
    props: {
        id: string;
        defaultCollapsed?: boolean;
        onCollapseChange?: (collapsed: boolean) => void;
    };
    // (undocumented)
    state: 'collapsed' | 'expanded';
}

// @public
export type SidebarSection = 'productTypes' | 'platforms' | 'projects' | 'packages';

// @public
export type SidebarTab = 'nodeDetails' | 'clusterDetails' | 'filters';

// @public
export function simpleClusterLayout(nodes: Array<{
    id: string;
    name: string;
    type: string;
}>, edges: Array<{
    from: string;
    to: string;
}>, centerX: number, centerY: number, options?: SimpleLayoutOptions): NodeCartesian[];

// @public (undocumented)
export interface SimpleLayoutOptions {
    // (undocumented)
    baseRadius?: number;
    // (undocumented)
    maxDepth?: number;
    // (undocumented)
    ringSpacing?: number;
    // (undocumented)
    testOffset?: number;
}

// @public
export interface SpatialEntity {
    // (undocumented)
    id: string;
    // (undocumented)
    radius: number;
    // (undocumented)
    x: number;
    // (undocumented)
    y: number;
}

// @public
export class SpatialHash<T extends SpatialEntity> {
    constructor(config: SpatialHashConfig);
    clear(): void;
    getNearby(entity: T): T[];
    getPotentialCollisions(): Array<[T, T]>;
    getStats(): {
        totalCells: number;
        totalEntities: number;
        avgEntitiesPerCell: number;
        maxEntitiesPerCell: number;
        minEntitiesPerCell: number;
    };
    insert(entity: T): void;
    insertMany(entities: T[]): void;
    queryRegion(minX: number, minY: number, maxX: number, maxY: number): T[];
}

// @public
export interface SpatialHashConfig {
    // (undocumented)
    bounds?: {
        minX: number;
        maxX: number;
        minY: number;
        maxY: number;
    };
    // (undocumented)
    cellSize: number;
}

// @public (undocumented)
export interface SuperEdge {
    // (undocumented)
    from: string;
    // (undocumented)
    to: string;
}

// @public
export interface SuperNode {
    // (undocumented)
    id: string;
    // (undocumented)
    members: string[];
}

// @public
export function tarjanSCC(adj: Map<string, string[]>): string[][];

// @public (undocumented)
export interface TransitiveResult {
    // (undocumented)
    edgeDepths: Map<string, number>;
    // (undocumented)
    edges: Set<string>;
    // (undocumented)
    maxDepth: number;
    // (undocumented)
    nodes: Set<string>;
}

// @public
export interface UIStore {
    activeTab: ActiveTab;
    enableAnimation: boolean;
    previewFilter: PreviewFilter;
    resetZoom: () => void;
    setActiveTab: (tab: ActiveTab) => void;
    setEnableAnimation: (enabled: boolean) => void;
    setPreviewFilter: (preview: PreviewFilter) => void;
    setZoom: (zoom: number) => void;
    toggleAnimation: () => void;
    zoom: number;
    zoomIn: () => void;
    zoomOut: () => void;
}

// @public
export function updatePositionMap<T extends CollisionEntity>(positions: Map<string, T>, alpha: number, damping?: number): void;

// @public
export function updatePositions<T extends CollisionEntity>(entities: T[], alpha: number, damping?: number): void;

// @public
export const useActiveTab: () => ActiveTab;

// @public
export const useCircularDependencies: () => string[][];

// @public (undocumented)
export const useDataStore: UseBoundStore<StoreApi<DataStore>>;

// @public
export const useEnableAnimation: () => boolean;

// @public
export const useFilters: () => {
    nodeTypes: Set<string>;
    platforms: Set<string>;
    origins: Set<string>;
    projects: Set<string>;
    packages: Set<string>;
};

// @public (undocumented)
export const useFilterStore: UseBoundStore<StoreApi<FilterStore>>;

// @public
export const useGraphEdges: () => {
    source: string;
    target: string;
}[];

// @public
export const useGraphNodes: () => {
    id: string;
    name: string;
    type: "app" | "framework" | "library" | "test-unit" | "test-ui" | "cli" | "package";
    platform: "iOS" | "macOS" | "visionOS" | "tvOS" | "watchOS";
    origin: "local" | "external";
    project?: string;
    targetCount?: number;
}[];

// @public (undocumented)
export const useGraphStore: UseBoundStore<StoreApi<GraphStore>>;

// @public (undocumented)
export const useHasActiveFilters: () => boolean;

// @public
export const useHoveredNode: () => string;

// @public
export const useIsNodeSelected: (nodeId: string) => boolean;

// @public
export const usePreviewFilter: () => {
    type: "nodeType" | "platform" | "origin" | "project" | "package" | "cluster";
    value: string;
};

// @public
export const useSearchQuery: () => string;

// @public
export const useSelectedCluster: () => string;

// @public
export const useSelectedNode: () => {
    id: string;
    name: string;
    type: "package" | "app" | "framework" | "library" | "test-unit" | "test-ui" | "cli";
    platform: "iOS" | "macOS" | "visionOS" | "tvOS" | "watchOS";
    origin: "local" | "external";
    project?: string;
    targetCount?: number;
};

// @public (undocumented)
export const useUIStore: UseBoundStore<StoreApi<UIStore>>;

// @public
export const useViewMode: () => "focused" | "both" | "dependents" | "impact" | "full" | "path";

// @public
export const useZoom: () => number;

// @public
export class VanillaMachine<T extends MachineSchema = MachineSchema> {
    constructor(machine: Machine<T>, userProps?: MachineUserProps);
    readonly computed: ComputedFn<T>;
    readonly ctx: BindableContext<T>;
    readonly machine: Machine<T>;
    readonly prop: PropFn<T>;
    readonly refs: BindableRefs<T>;
    readonly scope: Scope;
    send: (event: MachineEvent) => void;
    get service(): Service<T>;
    start(): void;
    readonly state: Bindable<T['state']>;
    status: MachineStatus;
    stop(): void;
    // Warning: (ae-forgotten-export) The symbol "SubscriptionCallback" needs to be exported by the entry point api.d.ts
    subscribe: (fn: SubscriptionCallback<T>) => void;
}

// @public
export const VIEW_MODE_VALUES: ("full" | "focused" | "path" | "impact" | "dependents" | "both")[];

// @public
export type ViewMode = z.infer<typeof ViewModeSchema>;

// @public
export const ViewModeSchema: z.ZodEnum<{
    full: "full";
    focused: "focused";
    path: "path";
    impact: "impact";
    dependents: "dependents";
    both: "both";
}>;

// @public
export interface ViewportBounds {
    // (undocumented)
    margin?: number;
    // (undocumented)
    maxX: number;
    // (undocumented)
    maxY: number;
    // (undocumented)
    minX: number;
    // (undocumented)
    minY: number;
}

```
