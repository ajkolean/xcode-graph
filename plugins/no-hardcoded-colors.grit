language js

// Match string literals containing hex colors (e.g., "#fff", "#ffffff")
or {
  `"$_"` as $str,
  `'$_'` as $str
} where {
  $str <: r"#[0-9A-Fa-f]{3,8}",
  register_diagnostic(span=$str, message="Avoid hardcoded hex colors. Use design tokens: var(--colors-*)", severity="error")
}
